<div>
  <h1 class="font-bold text-4xl">Categories#show</h1>
  <p>Find me in app/views/categories/show.html.erb</p>
</div>

<ul>
  <% @transactions.each do |transaction| %>
    <li>
      <%= "#{transaction.name} by #{transaction.user.name}" %>
    </li>
  <% end %>
</ul>
